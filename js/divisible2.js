// Programa que pide un número y dice si es divisible por 2
const num = parseFloat(prompt('Ingrese un número: '));
if (num % 2 === 0) {
  console.log('El número es divisible por 2.');
} else {
  console.log('El número NO es divisible por 2.');
}
